package com.lyz.common.mainstr.sub;

public class Sub {

	public static void main(String[] args) {

		// String str="dCcJnIOX:ZDPF:1";
		// String[] strarray=str.split(":");
		// for (int i = 0; i < strarray.length; i++)
		// System.out.println(strarray[i]);

		// Object value = "dCcJnIOX:ZDPF:1";
		// String str = value.toString();
		//
		// Map<String, Object> hm = new HashMap<String, Object>();
		// hm.put("code", str.substring(0, str.indexOf(":")));
		// hm.put("itemCode", str.substring(str.indexOf(":")+1));
		// System.out.println(hm.toString());
		// Integer a = null;
		// if (a == 1) {
		// System.out.println("1");
		// } else {
		// System.out.println("2");
		// }
		// ShortNode shortNode = new ShortNode();
		// shortNode.setLongitude(1.002f);
		// shortNode.setLatitude(3.346f);
		//
		// ShortNode shortNode2 = new ShortNode();
		// shortNode2.setLongitude(1.002f);
		// shortNode2.setLatitude(3.346f);
		// List<ShortNode> shortNodeList = new ArrayList<ShortNode>();
		// shortNodeList.add(shortNode);
		// shortNodeList.add(shortNode2);
		// String waypoints = "";
		// for (ShortNode nodegao : shortNodeList) {
		// waypoints += nodegao.getLongitude() + "," + nodegao.getLatitude() +
		// ",";
		// }
		// waypoints = "";
		// if (StringUtils.isBlank(waypoints)) {
		// System.out.println("dasdasd");
		// } else {
		// waypoints = waypoints.substring(0, waypoints.length() - 1);
		// System.out.println(waypoints);
		// }
		//
		// List<ShortNode> revokeList = null;
		// Collections.sort(revokeList, new Comparator<ShortNode>() {
		// @Override
		// public int compare(ShortNode o1, ShortNode o2) {
		// if (o1.getLevel() > o2.getLevel()) {
		// return 1;
		// }
		// if (o1.getLevel() == o2.getLevel()) {
		// return 0;
		// }
		// return -1;
		// }
		// });
		System.out.println(Integer.MAX_VALUE);

	}

}
